# 残響都市 — プロジェクト方針

人間と、あとから入った AI の両方が読める説明。詳細なファイル分担は [README.md](README.md)。

このリポジトリは、別セッションの AI が途中から読んでも迷わないことを優先する。
コードを読む相手は「昨日の会話を覚えていない AI」だと仮定する。

## やること（だけ）

iPhone のブラウザで歩ける、シード決定論の 3D 廃墟探索。文明名は 14×14×14、核と断片もそれぞれ 14×14×14。入口の光から通常離脱した断片だけを、連携 ID の記録に残す。

やらないこと: Rapier 等の物理エンジン、PC 専用操作、即時 INSERT の拾得、連携なし配信の許可。

## コーディング方針

### コメントに書くこと

- **何をしているかより、なぜそうしているか** を書く。
- 関数・ファイルの先頭で、役割・やらないこと・前提・失敗して捨てた案を短く書く。
- 一見不思議な行（灯火の霧距離、拾得を離脱まで待たせる、格子衝突、など）には、制約か事故の理由を残す。
- 名前で分かる処理（`return null` など）にはコメントを付けない。
- コメントは日本語。人間と AI の両方の第一言語を日本語に揃える。

### コメントに書かないこと

- コードを言い換えただけの説明（「変数に代入する」など）
- 将来やるかもしれない機能の予告
- 捨てた案の思い出話（「昔 Rapier を入れようとした」を長文で）。一行の理由で足りる

### 変更するとき

- 意図が変わったら、先にコメントを直してからコードを直す。
- コメントとコードが食い違っているなら、コメントが古い。コードに合わせてコメントを直す。
- 新しい制約を踏んだら、その場に一行で理由を残す。Issue やチャットにしか書かないと、次の AI は見えない。
- 語彙は `src/game/words.ts`、生成は `src/game/gen.ts`、描画は `src/game/echoes.ts`、HUD は `src/components/echoes-app.tsx`。混ぜない。
- 適用済みマイグレーションは編集しない。新しい制約は新しい SQL ファイル。
- `src/lib/auth/` は `email-password.ts` 以外を触らない。
